package storebackend.enums;

public enum MediaType {
    IMAGE,           // NEU: Allgemeines Bild (für Slider)
    PRODUCT_IMAGE,
    LOGO,            // Alias für STORE_LOGO (Frontend-Kompatibilität)
    STORE_LOGO,
    STORE_BANNER,
    OTHER
}
