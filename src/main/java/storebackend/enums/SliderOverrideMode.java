package storebackend.enums;

public enum SliderOverrideMode {
    DEFAULT_ONLY,    // Nur Default-Bilder anzeigen
    OWNER_ONLY,      // Nur Owner-Uploads anzeigen
    MIXED            // Beide mischen
}

