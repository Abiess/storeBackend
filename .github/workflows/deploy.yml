name: Deploy to VPS

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: 🔧 Build with Maven
        run: |
          chmod +x ./mvnw
          ./mvnw clean package -DskipTests

      - name: 📦 Prepare JAR for Deployment
        run: |
          # Find the built JAR file
          JAR_FILE=$(find target -name "*SNAPSHOT.jar" -o -name "storeBackend*.jar" | grep -v javadoc | head -n 1)

          if [ -z "$JAR_FILE" ]; then
            echo "❌ No JAR file found in target directory!"
            echo "📁 Contents of target directory:"
            ls -lah target/ || echo "target directory is empty or doesn't exist"
            exit 1
          fi

          JAR_SIZE=$(stat --format=%s "$JAR_FILE" 2>/dev/null || stat -f%z "$JAR_FILE" 2>/dev/null)
          echo "Found JAR: $(basename "$JAR_FILE") ($(numfmt --to=iec $JAR_SIZE 2>/dev/null || du -h "$JAR_FILE" | cut -f1))"

          # Create a stable name: app.jar
          cp "$JAR_FILE" target/app.jar

          if [ ! -f target/app.jar ]; then
            echo "❌ Failed to create target/app.jar"
            exit 1
          fi

          echo "✅ Created target/app.jar for deployment"
          echo "📁 Contents of target:"
          ls -lah target/

      - name: 🚀 Deploy JAR to VPS (/tmp)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "target/app.jar"
          target: "/tmp/"
          # remove the leading "target/" folder on extraction → /tmp/app.jar
          strip_components: 1

      - name: 📋 Copy Deploy Script to VPS (/tmp)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "scripts/deploy.sh"
          target: "/tmp/"
          # remove the leading "scripts/" folder → /tmp/deploy.sh
          strip_components: 1

      - name: ✅ Verify JAR Transfer
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "🔍 Checking for app.jar in /tmp..."

            if [ -f /tmp/app.jar ]; then
              APP_SIZE=$(stat --format=%s /tmp/app.jar 2>/dev/null || stat -f%z /tmp/app.jar 2>/dev/null)
              echo "✅ app.jar found! Size: $(numfmt --to=iec $APP_SIZE 2>/dev/null || du -h /tmp/app.jar | cut -f1)"
              exit 0
            fi

            echo "❌ /tmp/app.jar not found!"
            echo "📁 Files in /tmp:"
            ls -lah /tmp/ | head -30
            exit 1

      - name: 🔧 Setup VPS Environment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "🔧 Preparing VPS environment..."

            # Create base directory and subdirectories if they don't exist
            sudo mkdir -p /opt/storebackend
            sudo mkdir -p /opt/storebackend/backups
            sudo mkdir -p /opt/storebackend/logs

            # Create storebackend user if it doesn't exist
            if ! id -u storebackend > /dev/null 2>&1; then
              echo "Creating storebackend user..."
              sudo useradd -r -s /bin/bash storebackend
            fi

            # Move deploy.sh from /tmp to /opt/storebackend
            if [ -f /tmp/deploy.sh ]; then
              echo "Moving deploy.sh to /opt/storebackend..."
              sudo mv /tmp/deploy.sh /opt/storebackend/deploy.sh
            else
              echo "⚠️  /tmp/deploy.sh not found!"
            fi

            # Set permissions and ownership
            sudo chown -R storebackend:storebackend /opt/storebackend
            sudo chmod 755 /opt/storebackend

            # Ensure deploy.sh is executable
            if [ -f /opt/storebackend/deploy.sh ]; then
              sudo chmod +x /opt/storebackend/deploy.sh
            else
              echo "⚠️  /opt/storebackend/deploy.sh not found!"
            fi

            echo "✅ VPS environment ready"

      - name: 🔄 Execute Deployment
        uses: appleboy/ssh-action@master
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          envs: DB_PASSWORD,JWT_SECRET
          script: |
            # Export environment variables
            export DB_PASSWORD="${DB_PASSWORD}"
            export JWT_SECRET="${JWT_SECRET}"

            echo "🚀 Starting deployment script..."
            cd /opt/storebackend

            if [ ! -x ./deploy.sh ]; then
              echo "❌ /opt/storebackend/deploy.sh is missing or not executable"
              ls -lah .
              exit 1
            fi

            ./deploy.sh

      - name: 🏥 Final Health Check
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "Checking application health..."
            sleep 5
            curl -f http://localhost:8080/actuator/health || exit 1
            echo "✅ Application is running!"

      - name: 📊 Deployment Summary
        if: success()
        run: |
          echo "🎉 Deployment successful!"
          echo "✅ Application built"
          echo "✅ Deployed to VPS"
          echo "✅ Health check passed"
          echo "🌐 Your application is now live!"

      - name: ❌ Deployment Failed
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Please check the logs above for details."
          echo "The application may have been rolled back to the previous version."
