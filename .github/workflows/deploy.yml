name: Deploy Backend to VPS

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: 🔧 Build with Maven
        run: |
          chmod +x ./mvnw
          ./mvnw clean package -DskipTests

      - name: 🔍 Validate Flyway Migrations
        run: |
          chmod +x scripts/validate-migrations.sh
          ./scripts/validate-migrations.sh

      - name: 📦 Prepare JAR for Deployment
        run: |
          JAR_FILE=$(find target -name "*SNAPSHOT.jar" -o -name "storeBackend*.jar" | grep -v javadoc | head -n 1)

          if [ -z "$JAR_FILE" ]; then
            echo "❌ No JAR file found in target directory!"
            ls -lah target/ || true
            exit 1
          fi

          JAR_SIZE=$(stat --format=%s "$JAR_FILE" 2>/dev/null || stat -f%z "$JAR_FILE" 2>/dev/null)
          echo "Found JAR: $(basename "$JAR_FILE") ($(numfmt --to=iec $JAR_SIZE 2>/dev/null || du -h "$JAR_FILE" | cut -f1))"

          cp "$JAR_FILE" target/app.jar
          test -f target/app.jar

          echo "✅ Created target/app.jar for deployment"
          ls -lah target/

      - name: 🚀 Deploy JAR to VPS (/tmp)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "target/app.jar"
          target: "/tmp/"
          strip_components: 1

      - name: "🔍 Debug: list scripts + verify files"
        run: |
          echo "PWD: $(pwd)"
          echo "Repo files:"
          ls -lah
          echo ""
          echo "scripts folder:"
          ls -lah scripts || true
          echo ""
          echo "Check required files (Flyway-based):"
          for f in \
            scripts/deploy.sh \
            scripts/diagnose-database.sh \
            scripts/flyway-helper.sh \
            scripts/setup-postgres-user.sh \
            scripts/install-minio.sh \
            scripts/fix-db-password.sh \
            scripts/force-run-migrations.sh; do
            if [ -f "$f" ]; then
              echo "✅ $f"
            else
              echo "❌ MISSING: $f"
              exit 1
            fi
          done
          echo ""
          echo "✅ All required Flyway-based scripts present"

      - name: 📋 Copy scripts to VPS (/tmp)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "scripts/deploy.sh,scripts/diagnose-database.sh,scripts/flyway-helper.sh,scripts/setup-postgres-user.sh,scripts/install-minio.sh,scripts/fix-db-password.sh,scripts/force-run-migrations.sh"
          target: "/tmp/"
          strip_components: 1

      - name: ✅ Verify JAR Transfer
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            echo "🔍 Checking for app.jar in /tmp..."
            test -f /tmp/app.jar
            ls -lah /tmp/app.jar

      - name: 🔧 Setup VPS Environment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            echo "🔧 Preparing VPS environment..."

            sudo mkdir -p /opt/storebackend/backups
            sudo mkdir -p /opt/storebackend/logs
            sudo mkdir -p /opt/storebackend/scripts

            if ! id -u storebackend > /dev/null 2>&1; then
              sudo useradd -r -s /bin/bash storebackend
            fi

            # Move deploy.sh
            if [ -f /tmp/deploy.sh ]; then
              sudo mv /tmp/deploy.sh /opt/storebackend/deploy.sh
            fi

            # Move Flyway-based scripts
            for f in diagnose-database.sh flyway-helper.sh setup-postgres-user.sh install-minio.sh fix-db-password.sh force-run-migrations.sh; do
              if [ -f "/tmp/$f" ]; then
                sudo mv "/tmp/$f" "/opt/storebackend/scripts/$f"
                echo "✅ Moved $f"
              fi
            done

            sudo chown -R storebackend:storebackend /opt/storebackend
            sudo chmod 755 /opt/storebackend

            # Make scripts executable
            sudo chmod +x /opt/storebackend/deploy.sh || true
            sudo chmod +x /opt/storebackend/scripts/diagnose-database.sh || true
            sudo chmod +x /opt/storebackend/scripts/flyway-helper.sh || true
            sudo chmod +x /opt/storebackend/scripts/setup-postgres-user.sh || true
            sudo chmod +x /opt/storebackend/scripts/install-minio.sh || true
            sudo chmod +x /opt/storebackend/scripts/fix-db-password.sh || true
            sudo chmod +x /opt/storebackend/scripts/force-run-migrations.sh || true

            echo "✅ VPS environment ready (Flyway-based)"
            echo "📁 Files in /opt/storebackend/scripts:"
            ls -lah /opt/storebackend/scripts | head -50

      - name: 🗃️ Setup PostgreSQL Database (one-time, if needed)
        uses: appleboy/ssh-action@master
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          envs: DB_PASSWORD
          script: |
            set -e
            echo "🗃️ Checking PostgreSQL setup..."
            
            # Prüfe ob storeapp User existiert
            if sudo -u postgres psql -tAc "SELECT 1 FROM pg_roles WHERE rolname='storeapp'" | grep -q 1; then
              echo "✅ PostgreSQL user 'storeapp' exists"
            
              # Stoppe App um Locks zu vermeiden
              sudo systemctl stop storebackend || true
              sleep 2
            
              # WICHTIG: Setze Permissions VOR Flyway (verhindert V3 Lock)
              echo "🔐 Setting complete permissions (outside Flyway to avoid locks)..."
              cd /opt/storebackend
              export DB_PASSWORD="${DB_PASSWORD}"
              sudo -E ./scripts/fix-db-password.sh
            
              echo "✅ Permissions set"
            
              # ✅ NEU: Force-Run V5/V6 Migrations direkt auf DB
              echo "🔧 Checking and forcing V5/V6 migrations..."
              if [ -f ./scripts/force-run-migrations.sh ]; then
                chmod +x ./scripts/force-run-migrations.sh
                sudo -E ./scripts/force-run-migrations.sh
              else
                echo "⚠️  force-run-migrations.sh not found - will rely on Flyway"
              fi
            else
              echo "🆕 Creating PostgreSQL user and database..."
              cd /opt/storebackend
              export DB_PASSWORD="${DB_PASSWORD}"
              sudo -E ./scripts/setup-postgres-user.sh
            fi
            
            echo "✅ PostgreSQL ready - Flyway will handle schema migrations"

      - name: 🔄 Execute Deployment
        uses: appleboy/ssh-action@master
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          FLYWAY_BASELINE_ON_MIGRATE: "false"
          FLYWAY_REPAIR_ON_MIGRATE: "false"  # ✅ Zurück zu false - Repair wurde bereits ausgeführt
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          envs: DB_PASSWORD,JWT_SECRET,FLYWAY_BASELINE_ON_MIGRATE,FLYWAY_REPAIR_ON_MIGRATE
          script: |
            set -e
            export DB_PASSWORD="${DB_PASSWORD}"
            export JWT_SECRET="${JWT_SECRET}"
            export FLYWAY_BASELINE_ON_MIGRATE="${FLYWAY_BASELINE_ON_MIGRATE}"
            export FLYWAY_REPAIR_ON_MIGRATE="${FLYWAY_REPAIR_ON_MIGRATE}"

            cd /opt/storebackend
            test -x ./deploy.sh
            ./deploy.sh
            echo "📌 Flyway-related startup logs:"
            sudo journalctl -u storebackend -n 300 --no-pager | egrep -i "flyway|migrat|schema_history|validate|baseline" || true

            

      - name: 🏥 Final Health Check
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            echo "🏥 Warte auf Anwendungsstart (inkl. Flyway Migrationen)..."
            echo "⏱️  Timeout: 240 Sekunden (4 Minuten)"
            
            MAX_ATTEMPTS=48
            SLEEP_TIME=5
            
            for i in $(seq 1 $MAX_ATTEMPTS); do
              ELAPSED=$((i * SLEEP_TIME))
              echo "⏳ Versuch $i/$MAX_ATTEMPTS (${ELAPSED}s / 240s)..."
            
              if curl -f -s http://localhost:8080/actuator/health > /dev/null 2>&1; then
                echo ""
                echo "✅ Application is running!"
                echo "📊 Health Status:"
                curl -s http://localhost:8080/actuator/health | jq '.' 2>/dev/null || curl -s http://localhost:8080/actuator/health
                echo ""
                echo "⏱️  Started after ${ELAPSED} seconds"
                exit 0
              fi
            
              if [ $i -lt $MAX_ATTEMPTS ]; then
                sleep $SLEEP_TIME
              fi
            done
            
            echo ""
            echo "❌ Application nicht erreichbar nach 240 Sekunden"
            echo ""
            echo "═══════════════════════════════════════════════════════════"
            echo "📋 SYSTEMD STATUS"
            echo "═══════════════════════════════════════════════════════════"
            sudo systemctl status storebackend --no-pager --lines=50 || true
            echo ""
            echo "═══════════════════════════════════════════════════════════"
            echo "📋 LETZTE 200 LOG-ZEILEN (journalctl)"
            echo "═══════════════════════════════════════════════════════════"
            sudo journalctl -u storebackend -n 200 --no-pager || true
            echo ""
            echo "═══════════════════════════════════════════════════════════"
            echo "💡 TROUBLESHOOTING TIPPS"
            echo "═══════════════════════════════════════════════════════════"
            echo "1. Prüfe Flyway-Migration-Logs auf Fehler"
            echo "2. Prüfe DB-Verbindung: sudo /opt/storebackend/scripts/diagnose-database.sh"
            echo "3. Prüfe DB-Rechte: sudo /opt/storebackend/scripts/fix-db-password.sh"
            echo "4. Prüfe MinIO Status: sudo systemctl status minio"
            echo "5. Prüfe Schema-Validation: sudo journalctl -u storebackend | grep -i schema"
            echo ""
            exit 1
